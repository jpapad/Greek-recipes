{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "deploy:vercel": "vercel --prod",
    "deploy:netlify": "netlify deploy --prod",
    "prebuild": "npm run lint",
    "postbuild": "echo '✅ Build successful! Ready to deploy.'"
  }
}
